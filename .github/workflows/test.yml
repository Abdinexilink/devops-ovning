name: Min forsta pipeline

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Kolla ut koden
        uses: actions/checkout@v4

      - name: Skriv ut ett meddelande
        run: echo "Pipeline fungerar!"